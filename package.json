{
  "name": "busgo",
  "version": "1.0.0",
  "description": "BusGo - Bus Ticket Reservation System",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\" \"npm run dev --prefix admin\"",
    "build": "npm run build --prefix server && npm run build --prefix client && npm run build --prefix admin",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client && npm install --prefix admin",
    "seed": "npm run seed --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
